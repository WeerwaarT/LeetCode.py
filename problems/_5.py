class Solution:
    def longestPalindrome(self, s: str) -> str:
        longest_length = 1
        max_length = len(str)
        longest_str = ""
        for i in range(max_length):
            

        return longest_str